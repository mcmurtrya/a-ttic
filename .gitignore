# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Data and outputs (anchored to repo root so they don't shadow src/ttic_embeddings/data/, etc.)
/data/
/features/
/captions/
/checkpoints/
/wandb/
*.parquet
*.pkl

# Editor
.vscode/
.idea/
*.swp
*~
*.bak

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# Credentials / env
.env
.env.local

# LaTeX build artifacts (note: not `*.out` — pipeline.out is tracked)
*.pdf
*.aux
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
*.bbl
*.blg

# Claude Code session state (if it ever lands here)
.claude/
